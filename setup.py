# This is a dummy setup.py to enable editable install
# (pip install -e .)
# the actual dependency management is done by poetry
from setuptools import setup


setup(
    name='ogadf_schema',
    version='0.2.1.dev0',
)
